import logo from '.././logo.svg';

const Header = () => {
    return (
        <div className="header">
            <img src={logo} alt=""/>
            CORBIN
        </div>
    );
}

export default Header